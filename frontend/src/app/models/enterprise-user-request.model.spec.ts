import { EnterpriseUserRequest } from './enterprise-user-request.model';

describe('EnterpriseUserRequest', () => {
  it('should create an instance', () => {
    expect(new EnterpriseUserRequest()).toBeTruthy();
  });
});
