import { ClientUserRequest } from './client-user-request.model';

describe('ClientUserRequest', () => {
  it('should create an instance', () => {
    expect(new ClientUserRequest()).toBeTruthy();
  });
});
