import { CheckingResponse } from './checking-response.model';

describe('CheckingResponse', () => {
  it('should create an instance', () => {
    expect(new CheckingResponse()).toBeTruthy();
  });
});
